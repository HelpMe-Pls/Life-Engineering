# Skill Playbook Appendix — maintainer notes

*Extracted 2026-08-14 from `Claude Code Skill Playbook.md` so the playbook stays skim-grade for non-technical contributors. Nothing here is needed to use the recipes.*

## Machine facts (verified 2026-07-30 on Claude Code 2.1.220)

- Skill stacking active: ≤6 leading, consecutive `/commands`; all trailing text becomes ONE shared `$ARGUMENTS`; the first non-skill token ends the stack; an unknown leading token (typo) silently de-stacks everything.
- Hidden skills (most of Matt's pack: `implement`, `to-spec`, `to-tickets`, `wayfinder`, `triage`, `handoff`, `grill-with-docs`, `grill-me`, `improve-codebase-architecture`, `setup-matt-pocock-skills`, `ask-matt`, `wait-what`, `to-questionnaire`, `teach`) never appear in the model's registry; the deterministic trigger is typing them at message start, in colon form (`/mattpocock-skills:implement` …). Model-invoked (agent reaches for them alone): `tdd`, `codebase-design`, `domain-modeling`, `grilling`, `prototype`, `research`, `diagnosing-bugs`, `resolving-merge-conflicts`, `writing-for-agents`, and `wizard`.
- `/goal` (built-in, v2.1.139+) is a command, not a skill: nothing stacks with it; skill names inside a condition are data, never invocations — hidden skills stay invisible to the goal-working model. Pattern: two messages — skills/context first, then `/goal`; goal turns inherit the session. `/goal` is the until-condition looper; `/loop` is the interval/recurring one.
- Every skill is invoked in its plugin's colon form (`/mattpocock-skills:tdd`, `/superpowers:frontend-design`, …); stacking works the same with namespaced names. Bare names belonged to the retired npx-era copies. Note: `frontend-design` is NOT in the mattpocock-skills plugin (v1.2.3) — the playbook's UI-prototype variant uses `/superpowers:frontend-design`. `/compact` only at phase boundaries.
- Pack install (since 2026-08-14): the `mattpocock-skills` plugin from the `claude-plugins-official` marketplace — `/plugin install mattpocock-skills@claude-plugins-official`, updates via `/plugin` → Installed; the skill registry is built at session start. Retired npx era: `npx skills add mattpocock/skills -g -y` → `~/.agents/skills`, junctioned into `~/.claude/skills` (source in `~/.agents/.skill-lock.json`) — remove leftover copies to avoid duplicate menu entries; with colon-form boxes they can no longer be invoked by accident.

## Pack facts (v1.2.3, verified 2026-08-21 against SKILL.md sources + aihero.dev/skills)

- **Tracker-native publishing**: `/to-spec` publishes the spec **to the Issue tracker** (label `ready-for-agent`) and pins seams natively (existing > new, highest, ideal = one) — the box no longer restates seam policy, only the OPEN-marker walk-away conversion. `/to-tickets` publishes Issues with **native blocking links** and natively **quizzes the user** — the box pre-answers it ("skip the quiz", Law 3). `/wayfinder` charts the map as a `wayfinder:map` **issue** with decision-ticket children (`wayfinder:research|prototype|grilling|task`; claim = assign; frontier = open+unblocked+unclaimed) — never a map file.
- **`/implement`** natively drives `/tdd`, runs the full suite once, then `/code-review`, and commits to the *current* branch — branch/PR/spine moves are playbook overlay; recipe 5 therefore has a built-in review-lite before recipe 6's deep pass.
- **`/handoff` has no read-mode** — it only writes (OS temp dir, suggested-skills section, secrets redacted). Resume = new session + "Read the handoff file at `<path>` and continue." The old "type /handoff alone to resume" instruction was wrong and is removed.
- **`/prototype`** (reshaped in 1.2.0): logic branch = **one self-contained HTML file** a non-developer double-clicks (guided walkthroughs + free-play); UI branch = variants on one route behind a URL param. Kept as runnable evidence on a `prototype/<name>` branch, verdict captured in issue/ADR/commit — the DEC-* box aligns with this (links the branch + how to open).
- **`/triage`** roles: `bug`/`enhancement` + `needs-triage`/`needs-info`/`ready-for-agent`/`ready-for-human`/`wontfix`; NL invocation; AI-disclaimer on every tracker comment is native; external PRs off by default. Spine mapping stated in the playbook: ⚡ ≈ `ready-for-agent`, 🧑 ≈ `ready-for-human`.
- **New human-facing skills wired into the playbook**: `/wait-what` (re-pitch in ASD-STE100 using the repo's `CONTEXT.md` — Steer section), `/ask-matt` (router — table fallback), `/to-questionnaire` (decision belongs to someone else — Pauses), `/wizard` (model-invoked; generates a bash walkthrough for human-only steps — Pauses/Ship). `/teach` exists; not wired (out of SDLC scope).
- Per-repo `/setup-matt-pocock-skills` writes `docs/agents/issue-tracker|triage-labels|domain.md` + an `## Agent skills` CLAUDE.md block; its three interview sections are pre-answered in recipe 1's box (GitHub / default labels / single-context = root `CONTEXT.md` + `docs/adr/`). Grilling skills maintain `CONTEXT.md` natively; only the DEC-* *naming* overrides the pack default (see 2026-08-14 entry).

## Session-shape rules

- Driver + passengers: exactly one process skill per prompt (`implement`, `grilling`, `wayfinder`, `diagnosing-bugs`, `triage`, `prototype`, `code-review`); passengers are reference layers (`tdd`, `codebase-design`, `domain-modeling`, `frontend-design`, platform packs). Never two drivers. Sole carve-out: recipe 3's map stack `/wayfinder /grilling` — grilling rides as the map's interrogation layer.
- One phase, one context: review in its own session, each implement fresh, grill → spec → Issues unbroken; near the smart zone (~150k per the pack's PHASE-BOUNDARIES; stay conservative), update the spine and `/handoff` if mid-phase. Recipe 5's drain and recipe 6's small-PR loop consciously trade the fresh-context split for walk-away autonomy; big PRs escalate to B1–B3, where termination is a property of the spine file, never one session's say-so.

## Three laws for `/goal` conditions

1. **Displayed evidence only** — the transcript-only Haiku evaluator can check "⚡ section shown after the last edit", "test shown red then green", "PR URL pasted"; it cannot check "all issues resolved". The ⚡ queue is the scoreboard.
2. **Skill names in a condition are data** — load everything in M1; a goal loops ONE accumulating window, so big adversarial work escalates to the fresh-context variants.
3. **Pre-answer the answerable, pause on the strategic** — tactical policy comes from the repo's established git hygiene (written rules — repo docs + the `DEC-*` files in `docs/adr/` — first, merged-PR practice where they're silent; a call nothing answers is itself DEC-worthy; a merely-blocked item → 🧑 note, continue), but a DEC-worthy fork (hard to reverse, product intent, a new seam) pauses the run. Exception: a new seam mid-Requirements never pauses — `/to-spec` files it as an OPEN marker plus a 🧑 line. Every 🚶 goal carries the pause as an OR-arm — a paused run has *met* its goal; the "still unanswered by me" guard keeps a re-armed goal from free-riding on an already-answered pause.

## Enforcement map

The pack skills enforce the three CLAUDE.md blocks at the seams: `/to-spec` pins the test seams before any code; `/tdd` rides every build/fix stack (its tests.md + mocking.md carry the anti-patterns); `/implement` commits per green slice on the current branch; `/superpowers:finishing-a-development-branch` runs the merge/PR/cleanup decision; `/resolving-merge-conflicts` owns conflicts; `/code-review since <integration branch>`'s Standards axis checks conformance back at the branch point.

## Recipe 2a design notes

"Add a line of text" is a feature to a vibecoder, but grill → spec → Issues is disproportionate for it, so 2a goes straight to `/tdd /implement` (journey 2a → 7). Its guards: the standard pause valve plus an explicit outgrew-a-tweak trigger (product call / new seam / more than one small slice — the agent classifies size, never the vibecoder), a blocked → 🧑 arm, and it logs a **checked-off** ⚡ line linking the PR so recipe 7's Ship goal ("the shipped item's line deleted") stays satisfiable verbatim. `/tdd` stays in the stack for tweaks that do touch behavior; the testing-discipline block licenses zero tests for pure copy/styling. Review (6) is optional by design — the PR, the green suite, and the repo's branch protection are the net.

## Recipe 5 queue-fill design notes

**Hand-fill: added and removed the same day (2026-08-21).** The first request let the operator hand-write `- [ ] task` lines into ⚡, with a conversion-on-contact clause in both Variant boxes (link-less line → register as Issue → link → build). The operator then cut it in favor of interview-fill only: no-questions jots bypass exactly the questioning safeguard the queue exists to guarantee, and the conversion clause sat badly next to recipe 6's deliberately link-less ⚡ findings if flows ever crossed. All machinery reverted — conversion clauses and goal parentheticals out of Variant A/B, the spine template's ⚡ hint out, CLAUDE.md bullet-1's conversion sentence out. Kept: Variant A's "do 🔧 first" gate (an independent fix; B and 2a had it). Do not re-add hand-fill without re-reading this paragraph.

**Interview-fill** (the live mode — recipe 5's only fill path besides recipes 2/8): the entry mode between a quick thought and a recipe-2 feature. Driver `/to-tickets` with its native quiz **kept** — the exact inverse of recipe 2's "skip the quiz" pre-answer — scaled by an in-box rule (tweak-sized → no questions; real choices → numbered questions + recommended answer; ≤2 rounds). No `/grilling` stacked: the quiz is native and grilling is a second process skill (driver+passenger rule). Issues publish with native blocking edges — the operator's own edit labels them `ready-for-agent` OR `ready-for-human` accordingly — and mirror as ⚡ in build order, so Variant A's drain respects dependencies. Decision-items and operator-only items land 🧑, never ⚡; standard DEC pause. Badge 🧑→🚶 on recipe 2's send-then-goal shape; the goal quantifies over "every item in my batch" (transcript-checkable, same device as recipe 8's).

## Advanced variants (cut from the recipes for skimmability)

- Build on a platform with its own skill: stack it — `/tdd /implement /supabase:supabase …`.
- Independent ⚡ items in parallel: one session per item, each in its own worktree (`/superpowers:using-git-worktrees`).
- Deeper GitHub-PR review panel for B1: `/pr-review-toolkit:review-pr <N>` (silent-failure hunter, test-coverage analyst, type-design), same REPORT-ONLY contract.
- Walk-away frontier (recipe 3): `research`/`prototype`/`task` decision tickets need no operator — `/mattpocock-skills:wayfinder work through the map <URL> — resolve only frontier tickets that need no decision from me; a grilling ticket → 🧑 line naming it` + a `/goal` on "resolution comment + closed ticket + map pointer shown". Kept out of the playbook for skimmability; grilling tickets always stay 🧑.

## Housekeeping

- From superpowers keep four: `using-git-worktrees`, `finishing-a-development-branch`, `dispatching-parallel-agents`, `verification-before-completion`. The rest duplicates Matt's flow — prefer the typed commands.
- `/handoff` bridges a **conversation** (temp file, mid-phase); the spine bridges **work** (repo file, between items). Session minutiae never go into the spine.
- Ubiquitous language source: the pack's `CONTEXT.md` (in the plugin cache) — **Issue tracker**, **Issue**, **Decision ticket**, **Triage role**; "ticket" and "backlog" are retired terms. Playbook prose stays in ASD-STE100 Simplified Technical English; template boxes are machine-facing and never simplified.

## Revision history

- 2026-07-30 — rebuilt around the spine: Day-0 bootstrap, per-phase fire-and-forget entries with HITL pause valves, Matt Pocock's pack as the process layer.
- 2026-08-01 — prompts defer to each target repo's established git hygiene; DEC-GIT-FLOW demoted to a recorded fallback.
- 2026-08-14 — rewritten in ASD-STE100 for zero-code contributors: intent chooser table, explicit `<placeholders>`, HITL promise (start + end, pause as the exception), terms aligned to the pack's `CONTEXT.md` (Issue, never ticket), B1's PR checkout folded into its message, the setup prompt's playbook path made a placeholder.
- 2026-08-14 (later) — minimized for skimming: glossary dissolved into inline glosses, ✏️ fill / 📋 send-as-is box markers, recipes renumbered 1–11, agent-only reference blocks collapsed under `<details>`, maintainer content extracted to this appendix.
- 2026-08-14 (later still) — recipe 2a Quick change added: tweak-sized fast path (2a → 7) with an outgrew-a-tweak escape valve back to recipe 2; recipe 2 no longer claims "any size".
- 2026-08-14 (plugin switch) — install and invocation moved from the npx global pack to the `mattpocock-skills` plugin (claude-plugins-official marketplace, v1.2.3): every box uses `/mattpocock-skills:<skill>`; the UI-prototype variant uses `/superpowers:frontend-design`.
- 2026-08-14 (decisions split out) — `DEC-*` records left the spine's Reference section for `docs/adr/`, one file per call named for its ID (`docs/adr/DEC-GIT-FLOW.md`), per the pack's own ADR convention in `domain-modeling/ADR-FORMAT.md`. The pack's sequential `0001-slug.md` numbering is deliberately overridden — the `DEC-*` ID is the file name, so `/goal` conditions and prose can name a decision without a lookup. The spine now points at the folder and keeps only *un-ratified* decisions visible, as 🧑 lines; recipe 4's proposed verdict became `status: proposed` frontmatter plus a 🧑 line, which is what keeps its operator-confirms loop transcript-observable. Recipe 3's frontier box carries the naming clause explicitly — `domain-modeling` rides that stack and is the one skill that writes ADRs on its own, so its sequential default must be overridden in the box, not just in CLAUDE.md.
- 2026-08-21 (aligned to v1.2.3 ground truth) — deep pass against every SKILL.md in the installed plugin + all 25 aihero.dev/skills pages (see "Pack facts" above). Playbook drift fixed: spec/map/Issues publish to the **tracker** (recipe 2/3 boxes + goals rewritten; spine demoted to mirror, stated in How-it-works and the spine template); handoff resume corrected to "Read the file at `<path>`"; prototype box links the `prototype/` branch and how to open the HTML file; to-tickets quiz and setup's three interview sections pre-answered per Law 3; duplicated native policy cut from boxes (to-spec seams, tdd-restatement). Added: `/wait-what` + corrected handoff in Steer, `/ask-matt` fallback under the recipe table, `/to-questionnaire` + `/wizard` in Pauses, ⚡/🧑 ↔ `ready-for-agent`/`ready-for-human` equivalence, `CONTEXT.md` named in the decision docs, stage column in the recipe table, Lessons line added to the spine template (recipe 9's goal finally has a home), recipe 1's goal now checks `docs/agents/` in the diff. Recipe numbering, badges, `/goal` law compliance, and the DEC-* override kept verbatim.
- 2026-08-21 (hand-fill path) — recipe 5 gained "Fill the queue by hand": operator-jotted `- [ ] task` lines under ⚡ (and 🧑 for their own items), converted to Issues on contact by Variant A/B (see design notes above); routing row added to the table; spine template ⚡ hint + CLAUDE.md block 1 updated to match; both Variant goals now also demand the new Issue URL for hand-written items.
- 2026-08-21 (single-file handoff hardening) — the playbook is the ONLY artifact handed to contributors (this appendix stays maintainer-side, unlinked). Audited as a lone file and closed the gaps: new "Before your first recipe" section (operator/maintainer defined; maintainer-provisioned checklist; save-this-file + Finder/Explorer copy-path move; terminal-at-repo-folder move); Part A now installs `superpowers@claude-plugins-official` too — recipes 4/7 referenced `/superpowers:*` skills that Part A never installed, a real new-machine failure; walk-away mechanics stated (terminal stays open, computer awake; `claude --resume` recovery); recipe 7 previews deferred to the maintainer's command instead of assuming the vercel plugin; Esc-and-ask-the-maintainer escape hatch in Steer; micro-glosses (spec = the written plan, next-steps.md opens in TextEdit, template "HITL" spelled out).
- 2026-08-21 (interview-fill) — recipe 5 gained "Fill the queue by interview": `/to-tickets` as driver with its native quiz kept and scope-scaled (see queue-fill design notes), publishing labeled Issues with blocking edges and mirroring ⚡ in build order; decision/operator-only items → 🧑; recipe-2 send-then-goal shape; table row added.
- 2026-08-21 (hand-fill removed) — operator call: interview-fill is the only queue-fill path in recipe 5. Hand-fill block, its table row, the spine template's ⚡ hint, CLAUDE.md bullet-1's conversion sentence, and both Variant conversion clauses + goal parentheticals reverted; Variant A keeps "do 🔧 first". Rationale in the queue-fill design notes.
